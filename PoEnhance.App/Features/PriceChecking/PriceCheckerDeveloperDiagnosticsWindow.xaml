<Window x:Class="PoEnhance.App.Features.PriceChecking.PriceCheckerDeveloperDiagnosticsWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Price Checker Diagnostics"
        Width="320"
        Height="72"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStyle="None"
        Focusable="False"
        Background="Transparent">
    <Border Padding="8,6"
            Background="#F2111111"
            BorderBrush="#484848"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="StateText"
                       Foreground="#E5E5E5"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Text="Price Checker: Idle" />
            <TextBlock x:Name="DiagnosticCountText"
                       Grid.Row="1"
                       Margin="0,2,0,0"
                       Foreground="#AAAAAA"
                       FontSize="10"
                       Text="Diagnostics: 0" />
            <TextBlock x:Name="LatestDiagnosticText"
                       Grid.Row="2"
                       Margin="0,2,0,0"
                       Foreground="#C9A96E"
                       FontSize="10"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap" />
        </Grid>
    </Border>
</Window>
